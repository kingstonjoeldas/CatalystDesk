[project]
name = "catalystdesk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MacroSentry Builder", email = "kingstonjoel.m@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
catalystdesk = "catalystdesk:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
